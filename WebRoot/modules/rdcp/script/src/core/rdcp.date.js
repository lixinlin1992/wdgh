rdcp.util.date = {};

/**
 * 获取web服务器时间<br>
 * 如果web与db是同一台服务器,则使用getWebServerDate会更高效
 *
 * @param p {}
 */
rdcp.util.date.getWebServerDate = function (p) {

};

/**
 * 获取DB服务器时间<br>
 * 如果web与db是同一台服务器,则使用getWebServerDate会更高效
 *
 * @param p {}
 */
rdcp.util.date.getDBServerDate = function (p) {

};